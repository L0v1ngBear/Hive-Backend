server:
  port: 8080
  servlet:
    context-path: /api  # 保留你的 /api 前缀

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/hive?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: chaojimali.25
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  type-aliases-package: my.hive_back.module.*.model.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
redis:
  host: redis
  port: 6379
  database: 0
  lettuce:
    pool:
      max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
      max-idle: 8   # 连接池中的最大空闲连接
      min-idle: 0   # 连接池中的最小空闲连接
